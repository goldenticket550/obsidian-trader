node_modules
.next
.env.local
.env*.local
*.log
.DS_Store
# TypeScript incremental-compilation cache - generated automatically by
# `tsc` (tsconfig.json has "incremental": true), not real source. Was
# accidentally committed early on; removed from tracking, ignored going
# forward.
*.tsbuildinfo

.vercel
supabase/.temp/
.env*

# Local Codex review previews and temporary inspection helpers.
.codex-review/

# Reproducible local market-data assets; reports/hashes remain versionable.
data/archive/
data/replay/sessions/
data/replay/probe/
# Continuously rewritten local worker telemetry; durable reports remain tracked.
data/runtime-shadow/runtime-state-static-v1.json
data/runtime-shadow/supervisor-state.json
data/runtime-shadow/worker-liveness.json
data/runtime-shadow/last-run.json
